'use strict';

let follow = function(app) {
    var errs = app.errors;
    var User = app.models.user;
    
    let task = function(req, res) {
        let currentUser = req.session.user;
        
    };
    
    return task;
};

module.exports = follow;